import styled from "styled-components";

export const Message = styled.p`
  color: var(--color-status-02);
  font-size: 12px;
  letter-spacing: 0;
  margin-top: 4px;
  height: 15px;
  white-space: nowrap;
`;
